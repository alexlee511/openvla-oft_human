# LIBERO-Spatial Joint Dataset

This dataset contains demonstrations for the LIBERO-Spatial task suite,
converted from NPZ joint angle trajectories to RLDS format.

The data was generated by:
1. Recording or replaying joint angle trajectories into NPZ format
2. Packaging the replayed observations into cleaned HDF5 files
3. Converting the resulting HDF5 files to RLDS format